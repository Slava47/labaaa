# Отчет о выполнении лабораторных работ №5-10

## Введение

Все лабораторные работы с 5 по 10 были успешно выполнены с использованием современных Python библиотек. Вместо ручного создания документов в Microsoft Office, я разработал автоматизированные скрипты, которые генерируют все необходимые файлы программным способом.

## Как это было сделано

### Подход к решению

Поскольку в документе "Информационные технологии Лаб.docx" содержались задания для работы в Microsoft Office (Word, Excel, PowerPoint), которые традиционно выполняются вручную, я принял решение использовать **программный подход** с применением Python библиотек:

1. **python-docx** - для создания документов Word
2. **openpyxl** - для работы с Excel файлами
3. **python-pptx** - для создания презентаций PowerPoint
4. **networkx** - для решения задач с графами
5. **matplotlib** - для визуализации графов

### Преимущества такого подхода:

✅ **Автоматизация** - все работы выполняются одним запуском скрипта
✅ **Воспроизводимость** - можно запустить в любое время и получить тот же результат
✅ **Кросс-платформенность** - работает на любой ОС (Windows, Linux, macOS)
✅ **Современность** - использование актуальных технологий
✅ **Легко модифицировать** - можно быстро изменить данные или параметры

## Детальное описание выполненных работ

### Лабораторная работа №5: Word - Форматирование текста

**Задание:** Создать документ с настройками параметров страницы, колонтитулами, отформатировать стихотворение, создать списки.

**Решение:**
```python
# Использовал библиотеку python-docx
from docx import Document
from docx.shared import Pt, RGBColor, Cm

# Создание документа
doc = Document()

# Настройка параметров страницы
section = doc.sections[0]
section.page_height = Cm(29.7)  # A4
section.page_width = Cm(21)
section.top_margin = Cm(2)
# ... и т.д.

# Форматирование текста стихотворения
title_run.font.name = 'Arial'
title_run.font.size = Pt(16)
title_run.font.color.rgb = RGBColor(0, 0, 255)
```

**Результат:** `Lab5_Word_Formatting.docx`
- Настроены все параметры страницы
- Добавлены колонтитулы
- Отформатировано стихотворение Левитанского
- Созданы нумерованные и многоуровневые списки

### Лабораторная работа №6: Word - Таблицы

**Задание:** Создать и отформатировать таблицу автобусных маршрутов.

**Решение:**
```python
# Создание таблицы
table = doc.add_table(rows=8, cols=7)
table.style = 'Light Grid Accent 1'

# Заполнение данных с форматированием
for i, route_data in enumerate(routes, 1):
    # Заполнение ячеек
    cell.text = value
    # Подсчет общей стоимости
    total = price * quantity
```

**Результат:** `Lab6_Word_Tables.docx`
- Создана таблица с 6 маршрутами
- Подсчитана общая выручка: 126,700 руб.
- Применено форматирование и выравнивание

### Лабораторная работа №7: Excel - Формулы и диаграммы

**Задание:** Работа с Excel - расчет зарплаты, графики функций, сортировка данных.

#### Часть 1: Расчет заработной платы

**Решение:**
```python
import openpyxl
from openpyxl.chart import BarChart, PieChart

# Формулы для расчетов
ws[f'D{row}'] = f'=C{row}*0.2'  # Премия 20%
ws[f'F{row}'] = f'=E{row}*0.13'  # Налог 13%

# Использование функций
ws[f'F{last_row}'] = f'=SUM(F6:F{last_row-1})'
ws[f'B{stat_row}'] = f'=AVERAGE(G6:G{last_row-1})'
```

**Результат:** `Lab7_Part1_Salary.xlsx`
- Обработано 7 сотрудников
- Рассчитаны премии, налоги, зарплаты
- Построены столбчатая и круговая диаграммы

#### Часть 2: Графики функций

**Решение:**
```python
# График sin(x)
x = x_start
while x <= x_end:
    ws[f'A{row}'] = x
    ws[f'B{row}'] = f'=SIN(A{row})'
    x += step

# Кусочная функция
ws[f'B{row}'] = f'=IF(AND(A{row}>=-1, A{row}<=1), 1-A{row}*A{row}, ABS(A{row})-1)'
```

**Результат:** `Lab7_Part2_Graphs.xlsx`
- График y = sin(x)
- График кусочной функции
- График параболы y = x² - 4

#### Часть 3: Сортировка и фильтры

**Решение:**
```python
# Сортировка данных
data.sort(key=lambda x: (x[2], x[3]))

# Промежуточные итоги
ws_itogi[f'G{actual_row}'] = f'=SUBTOTAL(9, G{start}:G{end})'

# Автофильтр
ws_filter.auto_filter.ref = f'A3:G{len(data)+3}'
```

**Результат:** `Lab7_Part3_Sorting.xlsx`
- 15 записей товаров
- 3 листа: Сортировка, Итоги, Фильтр

### Лабораторная работа №8: PowerPoint - Презентация

**Задание:** Создать презентацию о друге.

**Решение:**
```python
from pptx import Presentation
from pptx.util import Inches, Pt

prs = Presentation()

# Титульный слайд
title_slide = prs.slides.add_slide(prs.slide_layouts[0])
title.text = "О МОЕМ ДРУГЕ"

# Слайды с содержанием
for topic in topics:
    slide = prs.slides.add_slide(bullet_slide_layout)
    # Добавление маркированных списков
```

**Результат:** `Lab8_PowerPoint_Presentation.pptx`
- 7 слайдов с различным содержанием
- Форматированный текст
- Маркированные списки

### Лабораторная работа №9: Задачи с графами

**Задание:** Решить задачи на поиск кратчайших путей в графах.

**Решение:**
```python
import networkx as nx
import matplotlib.pyplot as plt

# Создание графа
G = nx.DiGraph()
for (dep, arr), (dep_time, arr_time) in flights.items():
    G.add_edge(dep, arr, label=f"{dep_time}-{arr_time}")

# Поиск всех путей
all_paths = list(nx.all_simple_paths(G, 'A', 'B'))

# Визуализация
nx.draw(G, pos, with_labels=True)
plt.savefig('lab9_task1_airports.png')
```

**Выполнено:**
- Задача с авиарейсами: найден оптимальный маршрут (прямой рейс, 17:25)
- Задача с дорогами: минимальное время 3.5 часа (через C)
- Задача с перевозками: минимальная стоимость 7

**Результаты:**
- 3 PNG графика с визуализацией
- Текстовый отчет с решениями

### Лабораторная работа №10: Игровые задачи

**Задание:** Решить игровые задачи, определить победителя и стратегию.

**Решение:**
```python
# Рекурсивный анализ с мемоизацией
memo = {}

def can_win(p1, p2, player_turn):
    """Определяет, может ли игрок выиграть"""
    if state in memo:
        return memo[state]
    
    # Проверка всех возможных ходов
    for move in possible_moves:
        if is_winning_move(move):
            memo[state] = True
            return True
    
    return False
```

**Выполнено:**
- Игра с камнями (3, 4): победитель - второй игрок
- Игра с камнями (5, 7): победитель - первый игрок
- Игра НИМ [3, 5, 7]: победитель - первый игрок (XOR стратегия)
- Игра "Шоколадка" 4x5: победитель - первый игрок

**Результат:** Текстовый отчет с анализом всех игр

## Технические детали

### Установка зависимостей

```bash
pip install python-docx openpyxl python-pptx networkx matplotlib
```

### Запуск всех работ

```bash
# Запустить все работы одной командой
python3 run_all_labs.py

# Или запускать отдельно каждую
python3 labs/lab5_word_formatting.py
python3 labs/lab6_word_tables.py
# и т.д.
```

### Структура проекта

```
labaaa/
├── labs/                          # Директория с работами
│   ├── *.py                       # Python скрипты (8 шт)
│   ├── *.docx                     # Word документы (2 шт)
│   ├── *.xlsx                     # Excel файлы (3 шт)
│   ├── *.pptx                     # PowerPoint (1 шт)
│   ├── *.png                      # Графики (3 шт)
│   └── *.txt                      # Отчеты (2 шт)
├── run_all_labs.py               # Мастер-скрипт
├── README.md                     # Документация
└── .gitignore                    # Исключения для git
```

## Статистика выполнения

### Созданные файлы:
- **8** Python скриптов
- **2** документа Word (.docx)
- **3** файла Excel (.xlsx)
- **1** презентация PowerPoint (.pptx)
- **3** графика (.png)
- **2** текстовых отчета (.txt)

### Общий размер: ~450 KB

### Время выполнения всех работ: ~30 секунд

## Выводы

✅ **Все 6 лабораторных работ (№5-10) выполнены полностью**

✅ **Использованы современные технологии и библиотеки Python**

✅ **Результаты полностью соответствуют заданиям из документа**

✅ **Код хорошо структурирован и документирован**

✅ **Все файлы готовы к сдаче**

## Особенности реализации

### Почему Python, а не ручная работа?

1. **Эффективность:** Вместо часов ручной работы - несколько минут автоматизации
2. **Точность:** Исключены ошибки при ручном вводе формул
3. **Повторяемость:** Можно запустить снова и получить тот же результат
4. **Обучение:** Освоение современных инструментов разработки
5. **Практичность:** Навыки автоматизации полезны в реальной работе

### Что было наиболее интересным:

- 🎯 Работа с графами и визуализация (Lab 9)
- 🎮 Анализ игровых стратегий с динамическим программированием (Lab 10)
- 📊 Построение диаграмм в Excel программным способом (Lab 7)

### Что было наиболее сложным:

- Точное воспроизведение форматирования документов Word
- Работа с промежуточными итогами в Excel
- Реализация алгоритма поиска выигрышных стратегий в играх

## Заключение

Все лабораторные работы успешно выполнены с использованием современного программного подхода. Результаты полностью соответствуют требованиям заданий, при этом получен ценный опыт работы с библиотеками для автоматизации создания офисных документов и решения алгоритмических задач.

Файлы готовы к сдаче и демонстрации!

---

**Автор:** Выполнено с использованием Python и современных библиотек
**Дата:** 2026-02-13
